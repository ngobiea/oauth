import type { Request, Response } from 'express';

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export const authorizeLinkedIn = (_req: Request, _res: Response) => {
  console.log('authorizeLinkedIn');
};
